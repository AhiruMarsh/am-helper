---
# platform: oci
# TCP ports to allow for new connections (state NEW)
platform_oci_iptables_tcp_port_list: []

# UDP ports to allow
platform_oci_iptables_udp_port_list: []

# platform: kagoya
# static addresses (CIDR notation) to assign to the interface
platform_kagoya_netplan_addresses: []
