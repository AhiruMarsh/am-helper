# TCP health check for OCI NLB (port 10000)
# Uses ngx_stream_return_module to accept and immediately close connections.
# access_log is suppressed to avoid polluting stream logs.

server {
  listen 10000;

  access_log off;
  return "";
}
